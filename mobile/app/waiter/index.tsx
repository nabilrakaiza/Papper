import { View, Text } from 'react-native';

export default function WaiterHome() {
    return (
        <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
            <Text style={{ fontSize: 24, fontWeight: 'bold' }}>Hello World, Waiter</Text>
        </View>
    );
}