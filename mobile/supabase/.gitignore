# Local CLI state — contains the linked project ref, organization id and pooler
# host. Not secret (the project ref ships inside the app bundle) but it does not
# belong in a public repository.
.temp/
.branches/
